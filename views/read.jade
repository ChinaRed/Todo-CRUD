extends ./layout

block title

block content
  //- a(href="/todos/edit")
  //- button Edit To-Do
  .todo_info
    h2!=todo.title
    p.created_at!= todo.created_at
    button Edit
    p!= todo.description
  div
    form(action="/todos/#{todo._id}?_method=PUT",method="POST")
      input(type="text", name="title", value=todo.title)
      input(type="text",name="description", value=todo.description)
      button save
