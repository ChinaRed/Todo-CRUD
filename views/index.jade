extends ./layout

block title

block content 
  .new_todo.clearfix
    +newitem(todo)
  ul
    each todo in todos
      if todo.is_done === true
        li.todo.complete
          +listitem(todo, "checked")
      else 
        li.todo
          +listitem(todo)




